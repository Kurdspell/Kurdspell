<Window x:Class="Sample3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Sample3"
        mc:Ignorable="d"
        Title="Kurdspell Demo" Height="450" Width="800">

    <Window.Resources>
        <VisualBrush x:Key="SquigglyBrush" Viewbox="0,0,3,2" ViewboxUnits="Absolute" Viewport="0,0.8,6,4" ViewportUnits="Absolute" TileMode="Tile">
            <VisualBrush.Visual>
                <Path Data="M 0,1 C 1,0 2,2 3,1" Stroke="Red" StrokeThickness="0.2" StrokeEndLineCap="Square" StrokeStartLineCap="Square" />
            </VisualBrush.Visual>
        </VisualBrush>

        <Pen x:Key="SquigglyPen" Brush="{StaticResource SquigglyBrush}" Thickness="6" />
    </Window.Resources>

    <Grid>
        <RichTextBox FontSize="32" Visibility="Collapsed" x:Name="rich" TextChanged="Rich_TextChanged" ContextMenuOpening="Rich_ContextMenuOpening" KeyUp="Rich_KeyUp">
            <RichTextBox.ContextMenu>
                <ContextMenu x:Name="suggestionsMenu" />
            </RichTextBox.ContextMenu>
        </RichTextBox>

        <local:SpellTextBox IsSpellCheckEnabled="True" FontSize="32" x:Name="spellTextBox" AcceptsReturn="True" />
    </Grid>

</Window>
